.ui.fluid.vertical.accordion.menu
  - @resources.each do |resource_name, apis|
    .item
      .title
        .item.api__resource
          = resource_name.capitalize
          %i.dropdown.icon
      .content 
        .ui.fluid.vertical.menu
          - apis.each do |api|
            = link_to onebox_api_doc.api_path(version: @doc.version.name, tag: @current_tag.slug, resource_name: resource_name, method: api.method, url: api.friendly_id), class: 'item link-api-list', remote: false do
              .api__url
                %span.action{ class: "#{api.method.downcase}" }
                  = api.method
                %span.url 
                  = api.url
              .api__desc 
                = api.short_desc

